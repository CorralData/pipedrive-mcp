{
  "name": "pipedrive-mcp-cf",
  "version": "3.0.0",
  "private": true,
  "description": "Pipedrive MCP server on Cloudflare Workers (pure-TS, no container). OAuth provider bridged to CF Access OIDC.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
